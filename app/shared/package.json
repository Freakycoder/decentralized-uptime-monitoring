{
  "name": "shared",
  "version": "1.0.0",
  "description": "Contains shared socket connection object for websocket.",
  "main": "./output/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf output && tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf output"
  },
  "exports": {
    ".": "./output/index.js"
  },
  "files": [
    "output",
    "src",
    "README.md"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}